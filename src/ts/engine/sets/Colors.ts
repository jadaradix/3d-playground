import Color = require("../types/Color");

export default class Colors {
  public red: Color = new Color(255, 0, 0);
  public green: Color = new Color(0, 255, 0);
  public blue: Color = new Color(0, 0, 255);
};