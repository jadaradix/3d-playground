"use strict";
const chai = require("chai");
const expect = chai.expect;

describe("3d-playground", () => {

  it("asserts that true is true", (done) => {
    expect(true).to.equal(true);
    return done();
  });

});